import React from 'react';
import AppBar from '@material-ui/core';
import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles((theme)=> {

});

export default function PLM_AppBar() {
    const classes = useStyles();

    return (
        <div>
            <AppBar>
                
            </AppBar>
        </div>
    )

}